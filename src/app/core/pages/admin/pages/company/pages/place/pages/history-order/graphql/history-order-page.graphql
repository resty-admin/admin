query HistoryOrderPage($historyOrderId: String!) {
	historyOrder(id: $historyOrderId) {
		id
		orderNumber
		place {
			id
		}
		productsToOrders
		status
		table
		totalPrice
		type
		users
		startDate
	}
}
